{
  "name": "go-cd-dashboard",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "test": "./node_modules/mocha/bin/_mocha 'test/!(public)/**/*Spec.js'",
    "coverage": "istanbul cover --dir ./test/coverage --report json --print none --root src/ ./node_modules/mocha/bin/_mocha 'test/!(public)/**/*Spec.js' && istanbul report --root ./test/coverage --dir ./test/coverage html"
  },
  "dependencies": {
    "debug": "~2.0.0",
    "express": "~4.9.0",
    "request": "^2.72.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "istanbul": "^0.4.2",
    "mocha": "^2.4.5",
    "nock": "^8.0.0",
    "sinon": "^1.17.4",
    "sinon-stub-promise": "^3.0.1",
    "supertest": "^1.2.0"
  }
}
